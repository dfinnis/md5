test devices, pipes, symbolic links, sockets, etc.

static all files as possible
sort headers (inc libft)
norm
leaks
author

md5 & sha exit 0 on success, 1 on error
(at least one of the input files could not be read)

#### INPUT #### 
- flag: -s "string1" -s "string2"
- file as direct argument: ./ft_ssl md5 file
- standard in: -p OR echo "string" | ./ft_ssl md5

#### BONUS ####
1. parse commands from STDIN the same way that OpenSSL does
2. sha224
3. sha384
4. sha512
5. sha512224
6. sha512256
7. test.sh
